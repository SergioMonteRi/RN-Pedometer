import { StyleSheet } from "react-native";

const statsDataStyles = StyleSheet.create({
  label: {
    color: "white",
    fontSize: 20,
  },
  value: {
    fontSize: 45,
    color: "#AFB3BE",
    fontWeight: "500",
  },
});

export default statsDataStyles;
